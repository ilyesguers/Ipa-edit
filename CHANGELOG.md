# دليل التغييرات — الإصدار 5.0 🚀

> هذا الملف هو **الدليل الكامل لكل ما تغيّر** في هذا الإصدار، مرتّب حسب طلباتك العشرة.
> المشروع جاهز للنشر على Railway مباشرة.

---

## 1) ⭐ الدفع بنجوم تيليجرام (Telegram Stars) — يعمل بالكامل

طريقة دفع جديدة كلياً، بدون أي إعدادات إضافية في Railway — تستخدم نفس `BOT_TOKEN` الموجود عندك.

**رحلة الشراء:**
1. المستخدم يختار المنتج والمدة داخل المتجر، ويضغط زر **«⭐ الدفع بالنجوم»** الذهبي (يظهر السعر التقريبي بالنجوم قبل الدفع).
2. يُنشئ السيرفر رابط فاتورة رسمي من تيليجرام (`createInvoiceLink` بعملة `XTR`).
3. تفتح نافذة الدفع الرسمية داخل تيليجرام، والمستخدم يدفع بنجومه.
4. البوت يستقبل `pre_checkout_query` ويتحقق من الطلب والمبلغ والمستخدم خلال مهلة العشر ثوانٍ.
5. بعد نجاح الدفع (`successful_payment`) تُسلَّم المفاتيح **فوراً** في المتجر وفي رسالة من البوت، مع إشعار للإدارة.

**حماية الأموال (لا تضيع نجوم أي زبون أبداً):**
- لو نفد المخزون بين الفاتورة والدفع → **استرجاع تلقائي كامل** للنجوم عبر `refundStarPayment` مع رسالة واضحة للزبون والإدارة.
- لو انتهت صلاحية الطلب أو أُلغي قبل وصول الدفع → استرجاع تلقائي أيضاً.
- التسليم محمي من التكرار (Idempotent): نفس الدفعة لا تسلّم مرتين مهما تكررت التحديثات.
- **الاسترجاع من لوحة التحكم:** زر «استرجاع» على طلبات النجوم يعيد النجوم عبر تيليجرام نفسها (لا يضيف رصيد محفظة، حتى لا يُعوَّض الزبون مرتين)، ويبطل المفاتيح ويصحح الإحصائيات. نافذة الاسترجاع ورسالة النجاح تعرض الوصف الصحيح تلقائياً حسب طريقة الدفع (نجوم ↔ محفظة).

**الأدمن يتحكم بالسعر من الإعدادات** (لوحة التحكم ← الإعدادات ← المدفوعات):
- `stars_enabled` — مفتاح تشغيل/إيقاف الدفع بالنجوم.
- `stars_per_usd` — كم نجمة تساوي ١ دولار (الافتراضي 50⭐ = $1) مع **معاينة حية** ($1 / $5 / $10 تتحول فوراً إلى ⭐).

**الملفات المعنية:** `src/services/starsService.js` (جديد)، `src/bot/handlers/starsPayment.js` (جديد)، `src/api/routes/orders.js` (نقطة `POST /api/orders/stars` + `GET /api/orders/lookup/:orderNumber` لتتبع التسليم)، `src/models/Order.js` (حقول النجوم)، `src/bot/index.js` (تسجيل المستمعين)، `src/index.js` (`pre_checkout_query` ضمن `allowed_updates` للويبهوك).

---

## 2) 🌌 خلفية فضائية حية — بأداء عالٍ وبدون أي تقطيع

- **نجوم تتلألأ** (أكثر من 100 نجمة بأحجام وسرعات وميض مختلفة).
- **شُهُب (نيازك) تعبر السماء** كل ثانيتين إلى خمس ثوانٍ بذيل متدرّج.
- **رموز فضائية طافية** 👨‍🚀 🛸 🛰️ 🌙 تظهر كل ثوانٍ وتعبر الشاشة بهدوء.
- مبنية على `<canvas>` واحد بأريays جاهزة مسبقاً (بدون عناصر DOM إضافية) + `requestAnimationFrame`.

**لماذا لا يوجد Lag؟**
- دقة الرسم `DPR ≤ 1.75` حتى على الشاشات عالية الكثافة.
- **الإيقاف التلقائي** عند إخفاء التبويب (`document.hidden`) — صفر استهلاك بالخلفية.
- احترام `prefers-reduced-motion`: يعرض إطاراً ساكناً واحداً فقط.
- **طبقة السديم (الغمام الملون) مرسومة مرة واحدة** في Canvas خارجي مخبّأ ويُلصق كل إطار بـ `drawImage` واحدة بدل إعادة حساب 3 تدرجات شعاعية 60 مرة بالثانية.
- تنظيف كامل للمؤقتات والمستمعين عند إغلاق الصفحة.
- إصلاح خلل كان يجعل النجوم تُرسم أسود (غير مرئية) في أول إطار وفي وضع تقليل الحركة — لون النجوم أصبح يُضبط صراحة كل إطار.

**الملف:** `miniapp/customer/src/components/SpaceBackground.jsx` (جديد) + أنماطه في `index.css`.

---

## 3) 🔊 نظام صوتيات كامل + زر تحكم بالصوت

- **أصوات تفاعل خفيفة** مولّدة عبر WebAudio (بدون ملفات خارجية وبدون تحميل زائد): نقر، فتح، إغلاق، تبويب، نجاح، خطأ، عملة ⭐ الشراء بالنجوم… إلخ.
- **زر التحكم بالصوت في الهيدر:** لوحة منبثقة فيها **شريط تمرير للرفع/الخفض**، زر **كتم/تشغيل**، وأزرار سريعة 25% / 50% / 100% مع نسبة مئوية حية.
- الإعداد يُحفظ في المتصفح (`localStorage`) ويبقى بعد الإغلاق.
- نفس التحكم موجود أيضاً في تبويب «حسابي» (مفتاح كتم + شريط مستوى الصوت باتجاه RTL صحيح).
- فتح الصوت يتم تلقائياً عند أول لمسة (سياسة المتصفحات).

**الملفات:** `miniapp/customer/src/utils/sound.js` (جديد)، `miniapp/customer/src/components/VolumeControl.jsx` (جديد)، تحديث `Header.jsx` و`ProfileTab.jsx` و`BottomNav.jsx`.

---

## 4) 🖼️ صورة لكل شيء — منتج، قسم، لعبة، بانر

- مكوّن جديد موحّد **`ImagePicker`** في لوحة التحكم: رفع صورة جديدة **أو** الاختيار من مكتبة الوسائط الموجودة، مع معاينة وزر إزالة وسحب-وإفلات.
- **الأقسام:** صورة لكل قسم (تظهر في المتجر بدل الإيموجي مع سقوط تلقائي للإيموجي لو لا توجد صورة).
- **الألعاب داخل القسم:** أيقونة/صورة لكل لعبة.
- **المنتجات:** شعار **وبانر علوي** جديد يظهر في نافذة اختيار المدة.
- كل الرفع يمر عبر نقطة `/api/upload/image` الموجودة — لا يحتاج أي تغيير سيرفر.
- إصلاح نقطة `POST /api/categories` كي تقبل حقل `image` عند إنشاء قسم جديد (كان يُسقط عند الإنشاء فقط).

**الملفات:** `miniapp/admin/src/components/ImagePicker.jsx` (جديد)، `pages/Categories.jsx`، `pages/Products.jsx`، ومكوّنات العرض في متجر العميل.

---

## 5) 🎬 قوائم قابلة للطي بأنميشن + ستايل كرتوني جميل

- **التنقل بين الأقسام/الألعاب/المنتجات** أصبح طبقات متدرجة (Drill-down): كل مستوى يدخل بأنميشن سلس، مع أصوات ولمسات اهتزازية.
- **دليل المتجر والأسئلة الشائعة** أصبحت قوائم قابلة للطي **بأنميشن حقيقي** (ارتفاع متحرك + سهم دوّار) بدل `<details>` الجامدة.
- نمط `.collapsible` عام قابل لإعادة الاستخدام في أي مكان.
- **خط كرتوني عربي** جديد للعناوين: `Baloo Bhaijaan 2` (من Google Fonts مع `display=swap` حتى لا يتأخر التحميل)، مع بقاء خط Cairo للنصوص.
- بطاقات زجاجية (backdrop-blur) فوق خلفية الفضاء، وحركات hover لطيفة.

---

## 6) ✍️ عربية احترافية — بدون هجري وبدون «أسطوري»

- **التقويم الهجري أُزيل نهائياً** من كل المشروع: كل التواريخ الآن بالميلادي مع أرقام عربية (`ar-IQ`) في البوت ومتجر العميل **ولوحة التحكم** (6 مواضع كانت متبقية في اللوحة).
- حذف العامية والمصطلحات المحرجة من كل الرسائل: لا «يا برو / يا أسطورة / يا بطل / EZ / PLAY NOW» — استُبدلت بعبارات مهذبة ومهنية تناسب متجراً رسمياً.
- **ترحيل تلقائي آمن** (`src/services/database.js`): عند أول تشغيل، الإعدادات القديمة المحفوظة في قاعدة البيانات (لو كانت ما زالت بنصوص المصنع القديمة) تُستبدل بالنصوص الاحترافية الجديدة **دون المساس بأي نص خصّصه الأدمن بنفسه**.
- الحالات الخاصة أيضاً صارت محترمة: رسالة الحظر، وضع الصيانة، الاشتراك الإجباري بالقناة، رسائل الأخطاء الست، ترقية الأدمن.

---

## 7) 🌍 ترجمات مكتملة — 12 لغة بلا ثغرات

- إعادة كتابة `i18n.js` بالكامل: **كل مفتاح موجود في اللغات الـ12** (العربية، الإنجليزية، الفرنسية، الإسبانية، الألمانية، التركية، الروسية، الأردية، الهندية، الإندونيسية، البرتغالية، الصينية).
- سلسلة سقوط آمنة: اللغة المختارة ← الإنجليزية ← العربية، فلا تظهر مفاتيح خام للمستخدم أبداً.
- مفاتيح جديدة لتغطية كل ما يظهر على الشاشة: الدفع بالنجوم، انتظار التسليم، إلغاء/فشل الدفع، دليل الشراء، التحكم بالصوت، «يبدأ من»، «غير متاح»… إلخ.
- حتى البطاقات التي كانت تعرض «يبدأ من / غير متاح» ثنائية اللغة فقط أصبحت مترجمة بالكامل.

---

## 8) 🩹 إصلاح المشاكل المُزعجة بصرياً ومنع تداخل النصوص

- إزالة أي نصوص تختلط عربي/إنجليزي في سطر واحد بشكل مكسور؛ اتجاه RTL/LTR ثابت لكل لغة من الـ12.
- البطاقات والنماذج تحترم طول النصوص (قصّ آمن بـ ellipsis، وحد أدنى للعرض) فلا يحدث تداخل.
- نماذج لوحة التحكم الطويلة (الأقسام/الألعاب) تدعم التمرير داخل النافذة `max-h-[90dvh] overflow-y-auto` فلا تخرج عن شاشة الجوال.
- تعديل مدد المنتج في لوحة التحكم أصبح **تعديلاً مباشراً في مكانه** (زر ✏️ يفتح السطر للتعديل ثم حفظ) بدل الحذف وإعادة الإدخال.

---

## 9) 🧹 تنظيف المستودع من العناصر بلا معنى

- حذف `src/bot/handlers/shop.js` — ملف ميت (كان مُستورداً ولا يُسجَّل أبداً)، وإزالة استيراده من `src/bot/index.js`.
- حذف سكربت `setup` من `package.json` — كان يشير لملف غير موجود أصلاً (`src/utils/setup.js`).
- لا ملفات أخرى أُزيلت حفاظاً على استقرار التشغيل.
- رقم الإصدار: `4.1.0` ← `5.0.0` في كل من `package.json` و`/health` في `src/index.js`.

---

## 10) 🚂 جاهزية Railway

- بناء الواجهتين مُختبَر وناجح: `npm --prefix miniapp/customer run build` ✅ و`npm --prefix miniapp/admin run build` ✅.
- فحص صياغة كل ملفات السيرفر المعدّلة ✅ + اختبار تحميل للوحدات الجديدة ✅.
- لا يحتاج النجوم أي متغيرات بيئة جديدة — فقط `BOT_TOKEN` (موجود). راجع التعليق الجديد في `.env.example`.
- للتفعيل في وضع الويبهوك لا شيء إضافي: `pre_checkout_query` أُضيف تلقائياً لـ `allowed_updates`.

---

### ملاحظة للإدارة: كيف تفعّل النجوم وتسعّرها؟
1. افتح لوحة التحكم ← **الإعدادات** ← تبويب **المدفوعات**.
2. فعّل مفتاح **الدفع بنجوم تيليجرام**.
3. حدّد **سعر النجوم** (كم ⭐ = $1) — الافتراضي 50⭐/$1 ويناسب أغلب المتاجر.
4. احفظ. زر «⭐ الدفع بالنجوم» يظهر فوراً للزبائن داخل المتجر.
