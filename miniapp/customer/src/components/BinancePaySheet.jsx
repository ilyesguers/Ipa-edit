import React, { useEffect, useState } from 'react';
import { motion } from 'framer-motion';
import { QRCodeSVG } from 'qrcode.react';
import toast from 'react-hot-toast';
import useStore from '../store/useStore';
import { t } from '../i18n';
import PremiumIcon from './PremiumIcon';
import api from '../utils/api';

export default function BinancePaySheet() {
  const { currentOrder, submitPaymentProof, selectedDuration, locale } = useStore(); const [txHash, setTxHash] = useState(''); const [loading, setLoading] = useState(false); const [copied, setCopied] = useState(false); const [walletInfo, setWalletInfo] = useState(null); const [countdown, setCountdown] = useState(900);
  useEffect(() => { api.get('/payment/info').then((res) => setWalletInfo(res.data.data)).catch(() => {}); }, []);
  useEffect(() => { if (countdown > 0) { const timer = setTimeout(() => setCountdown((value) => value - 1), 1000); return () => clearTimeout(timer); } return undefined; }, [countdown]);
  const amount = currentOrder?.amount || selectedDuration?.price || 0; const address = walletInfo?.usdtWallet || (locale === 'en' ? 'Contact support' : 'يرجى التواصل مع الدعم'); const checkoutUrl = currentOrder?.checkoutUrl || address; const copy = (value) => navigator.clipboard.writeText(value).then(() => { setCopied(true); toast.success(t(locale, 'copied')); setTimeout(() => setCopied(false), 1800); });
  const submit = async () => { if (!txHash.trim()) return toast.error(t(locale, 'txHash')); setLoading(true); try { await submitPaymentProof(txHash); toast.success(t(locale, 'submitted') || t(locale, 'copied')); useStore.setState({ showBinanceSheet: false }); } catch (error) { toast.error(error.response?.data?.error || t(locale, 'failed')); } finally { setLoading(false); } };
  return <><motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="fixed inset-0 bg-black/90 backdrop-blur-md z-50" /><motion.div initial={{ y: '100%' }} animate={{ y: 0 }} exit={{ y: '100%' }} transition={{ type: 'spring', stiffness: 260, damping: 30 }} className="fixed bottom-0 left-0 right-0 z-50 bottom-sheet rounded-t-3xl max-h-[95vh] overflow-y-auto"><div className="flex justify-center pt-3 pb-2"><div className="w-10 h-1 rounded-full bg-[#333]" /></div><div className="px-4 pb-8 space-y-4"><div className="text-center"><PremiumIcon name="gem" size="2.8rem" className="text-[#f0b90b] animate-float" /><h2 className="text-xl font-black text-white mt-2">{t(locale, 'binance')}</h2><p className="text-muted text-sm">USDT — TRC20</p></div><div className={`mx-auto w-fit px-4 py-2 rounded-xl border font-mono text-lg font-bold ${countdown > 300 ? 'border-neon/30 text-neon bg-neon/5' : 'border-red/30 text-red bg-red/5'}`}>{Math.floor(countdown / 60).toString().padStart(2, '0')}:{(countdown % 60).toString().padStart(2, '0')}</div><div className="bg-[#1a1a1a] border border-border rounded-2xl p-4 text-center"><p className="text-muted text-sm">{locale === 'en' ? 'Exact payment amount' : 'المبلغ الدقيق للدفع'}</p><p className="text-3xl font-black text-[#f0b90b]">{Number(amount).toFixed(6)}</p><p className="text-white font-bold">USDT TRC20</p><button type="button" onClick={() => copy(Number(amount).toFixed(6))} className="mt-2 text-xs text-neon border border-neon/30 px-3 py-1 rounded-lg">{t(locale, 'copyAll')}</button></div><div className="flex flex-col items-center gap-3"><div className="bg-white p-4 rounded-2xl"><QRCodeSVG value={checkoutUrl} size={180} bgColor="#ffffff" fgColor="#000000" level="H" /></div><p className="text-xs text-muted">{locale === 'en' ? 'Scan with Binance' : 'امسح الكود بتطبيق Binance'}</p></div><div><p className="text-xs text-muted mb-2">{locale === 'en' ? 'Or send manually to:' : 'أو أرسل يدوياً إلى:'}</p><div className="bg-[#1a1a1a] border border-border rounded-xl p-3 flex items-center gap-2"><p className="flex-1 text-xs font-mono text-white break-all">{address}</p><button type="button" onClick={() => copy(address)} className="w-8 h-8 rounded-lg bg-neon/10 border border-neon/30 text-neon">{copied ? '✓' : <PremiumIcon name="copy" />}</button></div></div>{currentOrder?.checkoutUrl && <a href={currentOrder.checkoutUrl} target="_blank" rel="noreferrer" className="w-full py-3 rounded-xl font-bold text-center block bg-[#f0b90b] text-black text-sm">{locale === 'en' ? 'Open Binance' : 'فتح في تطبيق Binance'}</a>}<div><p className="text-sm font-semibold text-white mb-2">{locale === 'en' ? 'Payment proof' : 'إثبات الدفع'}</p><input type="text" inputMode="text" placeholder={t(locale, 'txHash')} value={txHash} onChange={(event) => setTxHash(event.target.value)} className="w-full bg-[#1a1a1a] border border-border rounded-xl py-3 px-4 text-sm text-white placeholder-muted outline-none focus:border-neon-blue/50" /></div><motion.button type="button" whileTap={{ scale: 0.97 }} onClick={submit} disabled={loading || !txHash.trim()} className="w-full py-4 rounded-2xl font-black text-base bg-neon-blue text-black disabled:opacity-40">{loading ? t(locale, 'loading') : t(locale, 'submit')}</motion.button><button type="button" onClick={() => useStore.setState({ showBinanceSheet: false })} className="w-full py-2 text-muted text-sm">{t(locale, 'cancel')}</button></div></motion.div></>;
}
